{
  "name": "puppeteer-playground",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm install && cd react-ui/ && npm install && npm run build && cd .. && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "puppeteer": "^1.0.0"
  }
}
